name: Generar Turno Penitenciario

on:
  schedule:
    # TEST: 21/01 23:00 Argentina = 22/01 02:00 UTC
    - cron: '0 2 22 1 *'
    # PRODUCCION: Jueves 23:00 Argentina = Viernes 02:00 UTC (espera hasta 00:00 del viernes)
    # - cron: '0 2 * * 5'
  workflow_dispatch:

jobs:
  generar-turno:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    
    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Instalar navegadores de Playwright
        run: playwright install chromium --with-deps

      - name: Crear carpeta downloads
        run: mkdir -p downloads

      - name: Ejecutar script
        timeout-minutes: 80
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_DESTINATARIO: ${{ secrets.EMAIL_DESTINATARIO }}
          TZ: America/Argentina/Buenos_Aires
          MODO_TEST: ${{ github.event_name == 'workflow_dispatch' }}
        run: python main.py

      - name: Subir PDF como artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: turno-pdf-${{ github.run_number }}
          path: downloads/*
          retention-days: 90
          if-no-files-found: warn
